# Copied by Docker Compose for variable substitution (not the Nest server/.env).
#   cp .env.example .env
JWT_SECRET=replace-with-a-long-random-string
ADMIN_SIGNUP_CODE=replace-with-a-campus-admin-code
